name = input("nameni kiriting : ")
last_name = input("last_name kiriting : ")
age = input("age kiriting : ")


config = {
    'first_name' : name,
    'last_name' : last_name,
    'age' : age
}

print(config)

